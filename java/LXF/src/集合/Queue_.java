package 集合;

import java.util.*;
public class Queue_ {
    //Queue有四类方法
    /*
    * 1. 获取队列长度 int size()
    * 2. 添加元素到队尾 boolean add(E) / boolean offer(E)
    * 3. 获取队首元素并从队列中删除 E remove()/E poll()：
    * 4. 获取队首元素但并不从队列中删除 E element()/E peek()：
    */
    public static void main(String[] args) {
        // LinkList既实现了List接口，又实现了Queue
        List<String> L = new LinkedList<>();
        Queue<String> q = new LinkedList<>();
    }
}
